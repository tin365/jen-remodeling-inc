# Copy to .env.local and fill in values. Never commit .env.local.
# For GitHub Actions: use Repository → Settings → Secrets and variables → Actions (Variables).

# Supabase (required for Projects, Reviews, Contact, Admin)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: error tracking (see docs/HOW_TO_FIX_AND_IMPROVE.md)
# NEXT_PUBLIC_SENTRY_DSN=https://...@sentry.io/...

# Optional: CAPTCHA (Cloudflare Turnstile) for forms
# NEXT_PUBLIC_TURNSTILE_SITE_KEY=your-site-key
